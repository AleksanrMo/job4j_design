
1.
Создал три объекта класса User c полями типа int  и String.
Объект User1 занимает 72 бита - 12(заголовок) + 40 (объект String) + 10 (содержимое String(каждый символ 2 байта)) + 4(int) = 66 округляем до 72
Объект User2 занимает 64 бита - 12(заголовок) + 40 (объект String) + 8 (содержимое String(каждый символ 2 байта)) + 4(int) = 64
Объект User3 занимает 64 бита - 12(заголовок) + 40 (объект String) + 8 (содержимое String(каждый символ 2 байта)) + 4(int) = 64
Итого 200 байт

2. 

Создал два пустых объекта класса User.
Каждый объект занимает по 12 байт округляем до 16
Итого 32(16*2) байт

3.Циклом начал заполнять память , при заполнении вызвался сборщик мусора.